<li id="placement_{% if placement is defined %}{{ placement.id }}{% else %}new{% endif %}">
    {{ form_widget(placement_form) }}
    {% if kparam('eval_active') == true %}<td class="small"></td>{% endif %}
    <ul class="actions">
        <li><button type="submit" title="Enregistrer les modifications" class="save">Enregistrer</button></li>
        <li><a href="{{ path('GCore_PAPlacementIndex') }}?{{ app.request.queryString }}" title="Annuler les modifications" class="cancel">Annuler</a></li>
    </ul>
</li>
