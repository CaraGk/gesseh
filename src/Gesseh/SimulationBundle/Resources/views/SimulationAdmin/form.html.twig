{{ form_start(form) }}
    <span class="inline">{{ form_widget(form.department) }}</span>
    <span class="inline">{{ form_widget(form.is_excess) }}</span>
    <span class="inline">{{ form_widget(form.active) }}</span>
    <span class="inline">{{ form_widget(form.Valider) }}</span>
    <span class="inline">{{ form_rest(form) }}</span>
{{ form_end(form) }}

